<UserControl x:Class="MediaSnap.Views.AudioSessionItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:MediaSnap.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:AudioSessionViewModel}"
             d:DesignWidth="320">

    <Border Padding="8"
            Margin="0,2"
            CornerRadius="6"
            Background="{DynamicResource SessionItemBackground}">

        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <!-- App icon -->
            <Image Source="{Binding AppIcon}"
                   Width="24" Height="24"
                   Margin="0,0,8,0"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   VerticalAlignment="Center">
                <Image.Style>
                    <Style TargetType="Image">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding AppIcon}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Image.Style>
            </Image>

            <!-- Fallback icon when no app icon -->
            <TextBlock Text="&#xE767;"
                       FontFamily="Segoe Fluent Icons"
                       FontSize="18"
                       Foreground="{DynamicResource TertiaryText}"
                       Margin="0,0,8,0"
                       VerticalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding AppIcon}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- App name -->
            <!-- ReSharper disable once Xaml.DynamicResourceError -->
            <TextBlock Text="{Binding AppName, FallbackValue='Audio'}"
                       Foreground="{DynamicResource PrimaryText}"
                       FontSize="13"
                       VerticalAlignment="Center" />

            <!-- Audio indicator -->
            <!-- ReSharper disable once Xaml.DynamicResourceError -->
            <TextBlock Text="&#xE767;"
                       FontFamily="Segoe Fluent Icons"
                       FontSize="11"
                       Foreground="{DynamicResource TertiaryText}"
                       Margin="8,0,0,0"
                       VerticalAlignment="Center" />
        </StackPanel>
    </Border>
</UserControl>
